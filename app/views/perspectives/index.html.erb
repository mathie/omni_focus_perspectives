<% content_for :title, 'All Perspectives' %>

<% content_for :toolbar do %>
  <%= link_to 'New perspective', choose_new_perspective_path, class: 'btn btn-primary' %>
<% end %>

<ul class="nav nav-tabs">
  <%= li_link_to_active 'All', perspectives_path %>
  <%= li_link_to_active 'Custom (Project-based)', custom_projects_perspectives_path %>
  <%= li_link_to_active 'Custom (Context-based)', custom_contexts_perspectives_path %>
</ul>

<div class="tab-content">
  <div role="tabpanel" class="tab-pane active">
    <%= paginate @perspectives %>

    <%= render @perspectives %>

    <%= paginate @perspectives %>
  </div>
</div>
